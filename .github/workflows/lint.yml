name: Lint

on: [push]

jobs:
  phpcs-lint:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v2
        - name: PHP Lint
          uses: php-actions/composer@v6
          with:
              command: run lint
